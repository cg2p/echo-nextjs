# notes

mkdir echo-next
cd echo-next
npm init -y
npm install --save react react-dom next isomorphic-unfetch
mkdir pages

package.json
```
"scripts": {
  "dev": "next",
  "build": "next build",
  "start": "next start"
}
```

npm run dev
